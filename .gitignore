# Personal config (contains sensitive info: email, credentials, personal data)
config/company-context.json

# Generated reports (open HTML in browser, or use /export for PDF)
*-report.html
data/achievements/**/*_report.md

# Personal config — each user has their own (copy company-context.template.json to get started)
config/company-context.json

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
